# spring.datasource.* (url, username, password, driver-class-name) is set dynamically as system
# properties by PostgresTestContainer when DancewithmeTestContainer starts it.
# Persistence dialect is also set there (PostgreSQLOnHsqldbCompatibilityDialect).
